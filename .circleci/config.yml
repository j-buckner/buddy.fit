version: 2
jobs:
  build:
    docker:
      - image: node:latest
    steps:
      - checkout
      - run: echo "build"
  test:
    docker:
      - image: golang:latest
    steps:
      - checkout
      - run: echo "test"
  push:
    docker:
      - image: google/cloud-sdk
    steps:
      - run: |
          echo $GCLOUD_SERVICE_KEY | gcloud auth activate-service-account --key-file=-
          gcloud --quiet config set project ${GOOGLE_PROJECT_ID}
          gcloud --quiet config set compute/zone ${GOOGLE_COMPUTE_ZONE}
      - run: apt install nodejs
      - run: apt install yarn
      - run: curl -o- -L https://yarnpkg.com/install.sh | bash
      - checkout
      - run: make deploy.app && make deploy.infra
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test
      - push

#       version: 2.0
#   jobs:
#     push_app:
#       docker:
#         - image: node:latest
#     push_infra:
#       docker:
#         - image: golang:1.12.10
# workflows:
#   version: 2
#   jobs:
#     jobs:
#       - push_app
#       - push_infra